import { i{{pascalCase name}} } from './{{pascalCase name}}.dto'

export const {{pascalCase name}} = ({ children }: i{{pascalCase name}}) => {
  return (
    <{{camelCase name}}>
      {children}
    </{{camelCase name}}>
  )
}